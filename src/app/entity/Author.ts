export default class Author {
  //  constructor(public id: number, public name: string, public authorEmail: string, public age: number) { }
    constructor(public name: string, public authorEmail: string, public age: number) { }
}

